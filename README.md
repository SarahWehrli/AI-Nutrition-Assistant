## Setup

This project uses environment variables for API credentials.

1. Copy the example file:
   ```bash
   cp .env.example .env
2.	Add your own API keys to .env.
3.	Start the web_server.py
4.	Open http://127.0.0.1:5000 in your browser
